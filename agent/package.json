{
  "name": "podium-docker-agent",
  "version": "1.0.0",
  "description": "Local agent that exposes a small, authenticated subset of Docker Desktop's API so Podium's cloud-hosted backend can manage containers on your machine.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dockerode": "^4.0.12",
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  }
}
